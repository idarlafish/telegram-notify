name = "telegram-notify-tail"
main = "src/index.ts"
compatibility_date = "2026-04-01"
account_id = "95c5c6e1c01ea2d0c9fab69ee9e28462"

# Tail consumer for the main Worker; see ../wrangler.toml tail_consumers.

[vars]
DEBUG_CHAT_ID = "671615920"
SOURCE_NAME = "telegram-notify"

# BOT_TOKEN secret: pipe ALERT_BOT_TOKEN from the tools repo's .env via
# `bunx wrangler secret put BOT_TOKEN --name telegram-notify-tail`.
